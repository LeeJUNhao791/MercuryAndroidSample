# WeNet 语音识别效果差异技术分析报告

本报告针对 WeNet 语音识别功能在 **手机端** 与 **AR 眼镜端 (骁龙 AR1 平台)** 表现出的效果差异进行深度分析。

## 1. 核心现象描述
同一份 APK 运行在手机和眼镜上，手机端的识别准确度、响应速度以及连续说话的稳定性均优于眼镜端。

## 2. 差异原因分析

### 2.1 算力架构与推理延迟 (Compute Latency)
WeNet 作为深度学习模型，对实时算力要求极高。
*   **核心性能差距**：手机通常搭载骁龙 8 系列处理器，拥有超大核（如 Cortex-X 系列），单核性能远强于专为功耗优化的骁龙 AR1。
*   **处理堆积**：由于眼镜端 CPU 推理速度较慢，如果每帧音频的处理时间超过其采样时间，会导致音频切片积压，从而产生“漏字”或识别逻辑断裂。

### 2.2 声学路径与信噪比 (Acoustic Path)
*   **拾音位置**：手机麦克风正对说话者嘴部，拾音直接。眼镜麦克风位于镜腿（太阳穴附近），虽然有三麦克风阵列，但其物理位置决定了它会捕捉到更多侧向环境噪音和皮肤传导的干扰。
*   **振动干扰**：AR1 的独家背靠背 4 扬声器系统在输出声音时，物理振动可能通过镜架传导至麦克风，干扰 AEC（回声消除）算法，进而损伤识别所需的原始语音质量。

### 2.3 硬件加速利用率 (Hardware Acceleration)
*   **推理后端**：手机端的 PyTorch Mobile/ONNX Runtime 能更成熟地调用 GPU 或 NNAPI。
*   **专用 DSP 难以触达**：骁龙 AR1 虽有专用音频 DSP，但通用的 WeNet 代码通常运行在 CPU 上，无法直接利用 AR1 内部专门用于加速 ASR 的专用算力单元，形成了“算力浪费”。

### 2.4 散热限制与降频 (Thermal Throttling)
*   AR 眼镜受限于极小的散热空间，在高强度 ASR 推理负载下，芯片温度会迅速上升。
*   AR1 平台的电源管理策略会为了保护硬件而下调 CPU 频率，导致识别准确率随使用时长增加而出现“断崖式”下跌。

### 2.5 系统调度与能效平衡
*   AR 眼镜系统通常采用更激进的后台限制策略来维持续航，这可能限制了 ASR 线程获取 CPU 资源的优先级。

---

## 3. 形象化比喻
**手机** 相当于一台“高性能台式机”，算力充沛且散热冗余大；
**眼镜** 相当于一台“超便携上网本”，核心优势在于极致的佩戴便携性、传感器集成和近场交互，但处理重负载离线 AI 任务时存在先天的物理限制。

## 4. 优化建议

1.  **模型量化**：确保眼镜端使用的是经过 `INT8` 甚至更低精度量化的模型，以减轻 AR1 CPU 的计算负担。
2.  **模型剪枝/蒸馏**：针对眼镜平台训练参数量更小的 Small/Tiny 版本模型。
3.  **专用后端适配**：研究如何利用骁龙 AR1 的专用语音加速指令集或 DSP 后端进行推理加速。
4.  **环境控制**：测试时保持眼镜扬声器静音，以排除背靠背扬声器对拾音系统的物理干扰。
